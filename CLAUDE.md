# CLAUDE.md

本文件面向在本仓库工作的 Claude 会话，提供跨会话的工程上下文。

## 项目是什么

easy-agent-team：面向团队的 AI 能力集中管理与分发平台（Skill / MCP 配置 / 环境变量 / 数据库账号 / 部署托管 / 人机求助与经验沉淀）。

**当前状态：P0–P3 全部路线图完成并全链路验证**——P0：环境变量 + Skill 管理 + 认证；P1：求助系统 + 平台 AI 接入 + 经验沉淀；P2：角色模板 + MCP 配置分发 + 数据库账号分配（真实建库建号）；P3：部署托管（Dokploy 挂载、CLI 端密钥扫描含平台指纹匹配、部署门禁携带报告、状态按需刷新）。server 221 个 e2e 用例全过（含 mock Dokploy 与 mock WebSocket 日志端点、mock OpenAI、mock 飞书机器人验签、真实 PG 建库）。环境变量支持**非敏感明文存储**（决策 23：`secret` 标记只改存储与展示、**授权模型不变**——非敏感值存 `value_plain`、有读取权限者清单/控制台直接明文可见，无权限者仍需申请；读取不落审计、不进指纹清单；数据库分配凭证仅 `DB_PASSWORD` 敏感、整组默认仅授权申请人；数据库分配删除后不再出现在列表；控制台补齐环境编辑/删除与部署项目编辑/删除入口）。求助通知已改为**只支持飞书群自定义机器人** webhook（决策 16：加签密钥由用户粘贴、平台不再生成 HMAC 密钥；决策 17：消息升级为飞书卡片——含「查看请求」按钮与「发送给 Agent」代码块，卡片构建在 `packages/shared/src/feishu-card.ts`、`scripts/test-feishu-card.mjs` 可实测；helper 登记含接收求助/接收回复两开关、能力描述可空），CLI 12 命令组 + MCP 工具（stdio 22 / HTTP 23）+ 控制台 11 页面均做过真实冒烟。内置「平台使用指南」Skill（`eat-platform-guide`，决策 11：内容在 `packages/shared/src/platform-guide.ts`，改内容须递增 `PLATFORM_GUIDE_VERSION`；sync-bundle 对所有用户注入首位，slug 保留）与 `eat sync` 新落地布局（决策 12：实际文件 `~/.agents/skills/` + 逐个软链 `~/.claude/skills/`，旧目录自动迁移；决策 14：安装范围参数，默认/`--global` 全局、`--project` 落当前项目 `./.agents/skills/` + 相对软链 `./.claude/skills/`、与 `--dir` 三者互斥）已实现，并用真实设备码登录 + sync 冒烟验证过（安装范围参数用 stub 平台端点做过全流程冒烟）。用户管理（建号/改角色/禁用/重置密码，管理员改密已完成；决策 19：开放注册——管理员开关 + 邮箱后缀限制，注册即登录）与 CLI 平台自托管分发（`/install.sh`+`/install/eat.js`+`/install/AGENT.md`+`/install/MCP.md`，不发 npm；控制台 `/install` 安装页含给 Agent 的一键复制指令，文案单一来源在 `packages/shared/src/install.ts`；决策 20：Agent 安装流程只装 CLI 不提 MCP，MCP 配置独立板块、面向无 shell 环境客户端）已落地——安装脚本在容器内真实执行验证过。**Windows 全链路已兼容**（决策 24：新增 `GET /install.ps1` + shim `eat.cmd`/`eat`（**决策 29：刻意不生成 `eat.ps1`**——PowerShell 里 `.ps1` 优先级高于 `.cmd`，撞上默认 `ExecutionPolicy=Restricted` 会让 `eat` 直接报「禁止运行脚本」，就是 npm.ps1 那个著名报错；不落 `.ps1` 则自然回落到不受执行策略约束的 `eat.cmd`，用户零操作）、PATH 写用户级环境变量不用 setx、两个安装脚本都把逻辑包进 `main()`/`Install-EatCli` 防管道截断；`eat sync` 在 Windows 上把 `.claude/skills` 的软链换成复制实文件；AGENT.md 给双平台命令并要求 Agent 先判断系统、安装页按 UA 嗅探分页签、MCP 指引补 `cmd /c eat mcp`；凭证文案统一为 `~/.eat/credentials.json`）——install.ps1 用容器内下载的 pwsh 7.4 真实执行+语法校验过，Windows 复制路径用伪装 `process.platform=win32` 跑通首次复制/软链迁移/版本刷新/清理全流程，安装页双 UA 双视口 Playwright 验证过。**建项目支持从 Dokploy 搜索选应用**（决策 27：`GET /api/dokploy/applications` 展平 `project.all`、防御式解析、与建项目同权限；应用可能挂在 `project.applications`（老版本）或 `project.environments[].applications`（新版本，真机实测），两种都认；控制台弹窗里 Popover + cmdk 按项目分组，搜索同时匹配应用名/容器名/id，Dokploy 停用时就地降级为手填；**2026-09-02 用容器内自建的 Dokploy v0.30.4 复验：该版本 `project.all` 只回 `applicationId`/`name`/`applicationStatus`，不含 `appName` 与 `description`**，防御式回落让清单照常可用，但「按容器名搜」在这个版本上等于失效，详见决策 27）。**部署失败能在平台里看到真实报错、并新增构建/运行日志命令**（决策 28：状态改以 Dokploy 构建记录为准并懒绑定到 `deployment.dokploy_deployment_id`，失败时把构建日志末尾 12 行写进 `error`；新增 `GET /api/projects/:slug/build-logs`（REST）与 `/run-logs`（**Dokploy 只有 WebSocket 一条路**，平台侧收敛成带 `tail=N` 的一次性读取，为此 server 引入 `ws`）；日志权限收紧到项目成员并落审计；CLI 把 `projects`/`deploy-status`/`deploy-list` 收进 `eat project` 名词组、新增 `build-logs`/`run-logs`，`eat deploy` 保持顶层，旧命令曾留隐藏别名，**决策 30 已直接删除**（不背兼容包袱：不需要兼容旧的 CLI 调用方式，旧客户端 `eat self-update` 即可更新）；MCP 补 `get_build_logs`/`get_run_logs`——此前文档与内置指南宣称的 `get_deploy_logs` 从未实现过。**已对容器内自建的 Dokploy v0.30.4 真机全链路验证**：触发部署→绑定构建记录→构建失败时 `eat project status` 直接打印 Docker 拉镜像报错→`build-logs`/`run-logs` 读到真实日志，控制台弹窗 Playwright 桌面/移动双视口验证过）。**部署记录已改为以 Dokploy 为唯一事实源**（决策 30：`deployment` 表删掉 `status`/`error`，只存业务元数据——谁触发的、带了什么检查报告；触发时把 `eat:<元数据id>` 写进 Dokploy 构建记录的 `description` 做精确认领（`application.deploy` 的 `title`/`description` **v0.25.0 起支持**，更老的版本静默丢弃、自动降级为按时间推断并标 `inferred`）；**Dokploy 每个应用只保留最近 10 条构建记录**（`removeLastTenDeployments` 硬编码不可配、连日志一起删），所以默认视图就是它还留着的那些、`--all` 才从平台元数据看完整历史（被清理的显示 `archived`）；**在 Dokploy 侧直接触发的部署也会列出来并标「未经平台密钥扫描」**，绕过门禁从此可见；排队中的部署读 `deployment.queueList`。两条踩过坑的铁律：**已认领过的元数据既不参与时间推断、也不因「刚触发不久」退回 queued**，否则会把别人在 Dokploy 侧点的部署冒认成平台部署。API 破坏性变更：`GET /api/deployments/:id` 删除，改项目内 `GET /api/projects/:slug/deployments/:id`）。**部署托管的实体已从「项目」改为「应用」（决策 31，2026-09-03）**：与 Dokploy 的 application 一一对应；成员自助创建（Web / `eat app create` / MCP `create_app`）——填 Git 地址与构建方式，服务端按 `application.create` → `saveGitProvider`（绑管理员配置的 SSH key）→ `saveBuildType` 三步走、任一步失败即删掉刚建的 application 回滚；构建方式只开放 `dockerfile`（Dockerfile 路径 + 构建上下文）与 `static`（发布目录 + SPA；**不跑构建命令**，仓库里需直接有产物）；管理员在「系统设置 → Dokploy」选自助建应用的落点（Dokploy 项目 / 环境 / SSH key，从 Dokploy 现拉下拉清单），用户侧不再感知 Dokploy 的项目概念；管理员仍可挂载既有 application（`POST /api/apps/mount`，`managed=false`，构建配置归 Dokploy、删除只解绑）。**部署授权**：成员自建的应用首次部署被拒（`DEPLOY_NOT_APPROVED`）并留痕，管理员在控制台授权一次后永久有效。**应用 env**：`eat app env pull|push [--build]` / MCP `get_app_env`/`set_app_env` / 控制台详情页直接读写 Dokploy 上的运行时 env 与构建时 buildArgs，推送整体覆盖、只回 key 级差异（解析在 `packages/shared/src/dotenv.ts`）。控制台应用详情页有「部署」按钮：不做本地扫描，请求显式 `source=console`，记录标「控制台 ⚠ 未做密钥扫描」。**不需要兼容旧的 CLI 调用方式（用户 `eat self-update` 即可更新）、数据库存量数据原地搬迁**：`/api/projects`→`/api/apps`、`eat project`→`eat app`、`list_projects`→`list_apps`（各工具 `project` 参数改 `app`）、表 `project`/`project_member`→`app`/`app_member`；迁移 `0010_app_entity` 把 `project`→`app`（保留 id）、`project_member`→`app_member`、`deployment.project_id`→`app_id` 搬完再删旧表（平台已在线上使用，不能删数据），存量项目按「管理员挂载的既有应用」处理（`managed=false`、`build_type=NULL`、`deploy_approved=true`）；`eat sync --project` 指本地代码目录，不动。server 模块拆成 `DokploySettingsService`（接入配置 + client 工厂）/ `AppsService`（应用 CRUD、授权、env）/ `DeployService`（部署记录、日志、指纹），路由分 `apps.controller.ts` 与 `deploy.controller.ts`。CLI 升 0.5.0、平台指南升 8。**已对容器内自建的 Dokploy v0.30.5 真机全链路验证**（自助建两种应用 → 授权门禁 → `eat deploy` 构建成功起容器 → env 推拉 → 控制台双视口冒烟）。**v0.30.5 的坑**：Git 来源的构建一结束就把构建记录 title/description 覆盖成提交信息，`eat:<id>` 认领标记只在排队/构建期间存在——`deployment.claim` 列记下首次认领的方式，靠回写 id 再认时沿用，别把精确认过的显示成「按时间推断」。**建应用时自动分配域名（决策 32）**：管理员在「系统设置 → Dokploy」配 `domain_suffix`（标准化：去协议 / `*.` / 大小写，只认 RFC 1123 主机名）+ `domain_https`，成员建应用时第四步 `domain.create` 绑 `<slug>.<后缀>`（请求体最小集对 v0.30.5 真机验证；失败同样整体回滚），`app` 表记 `domain` / `domain_https` / `dokploy_domain_id`；**域名流量转发到容器端口**——`app.port` 由 dockerfile 应用的创建者声明（默认 3000，`--port` / MCP `port` / 控制台字段），static 固定 80，端口变化时 `domain.update` 回写（它的 `host` 必填）；配了后缀时 slug 必须能当 DNS label（不以连字符结尾、≤63）；`AppInfo` 加 `port` / `domain` / `url`。只影响新建应用，存量与挂载的不动。CLI 升 0.5.1、指南升 9、迁移 `0011_app_domain`。**Dokploy 不出现在面向 AI 的输出里（决策 33）**：CLI 帮助 / 输出、MCP 工具描述 / 返回、内置指南、服务端透传的报错一律说「部署后台」或直接说事，CLI 不打印 `dokployApplicationId`、MCP 返回前剥掉它；部署来源枚举改 `platform | external`，错误码改 `DEPLOY_BACKEND_UNAVAILABLE` / `DEPLOY_BACKEND_UNCONFIGURED`；控制台管理员板块标题改「部署后台接入（Dokploy）」，Dokploy 这个词只留在那里和人看的文档里。CLI 升 0.5.2、指南升 10。**`eat skill export <slug>` 可把平台上任一可见 skill（含未订阅的、含内置指南）下载成普通目录**：复用既有的 `GET /api/skills/:slug`、不动服务端；与 sync 落地的区别是不写 `.eat-meta.json`（导出的是可自由编辑再 push 的工作副本，别让 sync 接管），目标目录非空必须 `--force` 且只覆盖同名文件、不清空目录。CLI 升 0.5.3、指南升 11。**推送不再自动订阅（决策 34）**：`SkillsService.push()`（CLI push / 网页创建 / 经验沉淀共用）不再把推送者订阅上去——旧行为会让「退订自己的 skill → 推个新版本 → 又被订回来」，退订意愿留不住；经验沉淀改为按 `grantedToRequester` / `grantedToHelper` 各自显式插订阅、且不再删未勾选那方的既有订阅；可见性与编辑权不受影响（`canSee`/`canManage` 按 ownerId 判），变的只是进不进 `eat sync` 范围；CLI push 后曾按返回的 `subscribed` 提示订阅命令，**决策 40 已去掉这句提示**（刻意不订阅自己 skill 的作者每推一次被劝一次，属噪音；规则留在内置指南速查表里），服务端返回的 `subscribed` 字段不动；CLI 升 0.5.9、指南内容未变。顺带修掉更新检测用例里的空串自比（指纹用例打的是 @Public 的 `/api/health`，per-user 指纹在无 authUser 时根本不下发）。CLI 升 0.5.4、指南升 12。**CLI / Skill 更新提示已就位**（决策 26：检测走响应头搭车——服务端对带 `x-eat-client` 的请求回 `x-eat-cli-version` + `x-eat-skill-version`，零额外请求；Skill 指纹是 per-user 的 sorted(`slug@version`) FNV-1a 哈希，覆盖出新版本/新增订阅/退订三类变化；提示只走 stderr、不做 TTY 判断、按版本去重、显式声明不影响本次结果；新增 `eat self-update` 与 `GET /install/version.json`；MCP 侧改挂工具返回的独立内容块；`EAT_NO_UPDATE_NOTIFIER=1` 可关。CLI 版本唯一事实源在 `packages/shared/src/version.ts`，package.json 由单测断言同步；平台指南 Skill 版本升到 5）。**控制台 UI 已从 Ant Design 全量迁移为 Tailwind CSS v4 + shadcn 风格组件**（决策 21：组件源码内置 `apps/web/src/components/ui/`，表单栈 react-hook-form、消息 sonner、搜索选择 cmdk，移除 antd/dayjs；桌面左侧分组侧边栏 + 移动端抽屉导航，表格按断点隐藏次要列做手机适配；全部 16 页面经 Playwright 桌面/移动双视口截图与交互冒烟验证。注意：项目是 React 18，ui 组件必须用 forwardRef，不能学 shadcn 官方 React 19 的 ref-as-prop 写法。决策 22：多板块页面——求助/数据库/权限申请——改页内 tabs 布局，`components/ui/tabs.tsx` + `lib/use-tab-param.ts` 同步 URL `?tab=`；低频配置收进页头按钮弹窗——求助页「可求助登记」、用户页「注册设置」，按钮带状态圆点）。**README 里有产品截图与 CLI 录屏了（决策 35）**：`scripts/demo/` 下是可复现的演示数据与截图 / 录屏工具，产物在 `docs/assets/screenshots/` 与 `docs/assets/demos/`，全集页 `docs/screenshots.md`，重跑步骤与坑见 `scripts/demo/README.md`（演示 Git 服务必须 smart HTTP、基础镜像先 `docker pull`、录屏有先后依赖、截图放最后拍）。原则是画面里的东西必须是真的：数据走平台自己的 API 写入、命令真跑一遍录。录制时顺手修了两个 CLI 问题（升 0.5.5）：`eat env pull <环境>` 在一个变量都没权限时会什么都不打印（改为回落到清单给出申请引导）、密钥扫描的候选 token 正则把 `=` 收进字符集导致 `KEY=<密钥>` 这种最常见的泄漏形式匹配不到指纹（`=` 改为只当结尾 padding）。**SKILL.md 的 frontmatter 现在按 YAML 子集解析（决策 36）**：`description: >-` 这类块标量写法此前被那版一行一匹配的正则取成 `>-` 本身，描述等于废了；解析器提到 `packages/shared/src/frontmatter.ts` 三端共用（块标量 `>`/`|` 含截断与缩进数字、单双引号跨行标量、纯标量续行、CRLF/BOM/整行注释/`...`，嵌套结构的值跳过、行内 `#` 不当注释），服务端 `SkillsService.push()` 在描述为空或只是块标量指示符时回退到正文 frontmatter（旧 CLI 推的 `>-` 推一次新版本就自动修好），控制台创建 Skill 支持粘贴整份 SKILL.md 自动回填空着的字段，经验沉淀合成 frontmatter 改用 `toYamlScalar()` 写值；`|` 保留块解析出的**多行描述**在展示端也要处理——控制台详情页的触发描述用 `whitespace-pre-line`（列表页本就是 truncate 单行，不用改）、编辑框放到 4 行，CLI 的一行一条清单（`skill list` / `ask targets`）用 `oneLine()` 折行并按显示列宽（汉字算两列）截断到 200 列、`eat skill list --full` 不截断；**存量数据靠 `pnpm db:migrate` 里的幂等订正修**（`apps/server/src/db/repair-descriptions.ts`：描述为空或只是块标量指示符的行，按当前版本 SKILL.md 正文重新解析，不动 `updated_at`）——服务端回退只在 push 时生效，线上先有的 skill 不重新部署就会一直显示 `>-`。CLI 升 0.5.7。**Skill 有捆绑模式、清单可筛选分页、订阅者可管理（决策 37）**：管理员把 skill 设为**捆绑**（`skill.bundled`，迁移 `0012_skill_bundled`）后，它对所有成员恒为已订阅、退订直接回 400 `SKILL_BUNDLED`、`eat sync` 总会带上（`relation: 'bundled'`）；**管理员自己是例外**，仍按自己的订阅记录算——这条是需求的核心，改动有效集合时别把管理员也算进去。捆绑**要求团队可见**，开捆绑与改私有两个方向都按「结果状态」校验；只有管理员能改这个开关（作者对自己的 skill 也不行）。`GET /api/skills` **破坏性地**改成分页信封 `{items,total,page,pageSize}` + `q`/`scope`/`kind` 筛选（pageSize 上限 1000，照决策 30 不留兼容层，控制台与 CLI 同步改；`Templates.tsx` 的选择器用 `pageSize=1000` 取全量）；控制台每页 20 条、可切 50/100、页码+跳页且筛选与页码同步到 URL（`lib/use-query-params.ts` + `components/pagination.tsx`），CLI 按 gh 的做法默认 100 条、`--limit` 最多 1000、**不翻页**（AI 拿到半页会当成全部下结论），配 `--search`/`--scope`/`--kind`。订阅**人数**（口径 = 有效同步人数，只算启用中的用户，人数与明细共用 `effectiveSubscribers()`）全员可见；**名单与代订阅仅管理员**（`GET/POST /api/skills/:slug/subscribers`、`DELETE …/subscribers/:userId`），代订阅拒私有 skill 与禁用用户、捆绑 skill 拒单独增减。UI 按频次分层：列表页只放搜索/筛选/订阅，捆绑开关收进详情页「编辑元信息」弹窗，订阅者名单与「添加订阅者」收在详情页下方的管理员卡片。顺带修掉「模板派生的 skill 在清单显示已订阅、详情页却显示未订阅」和窄屏下第一列 `truncate` 把表撑出横向滚动两个问题。CLI 升 0.5.8、指南升 13。**Skill 列表页已重设计为「双行清单」（决策 38）**：名称 + 触发描述排成阅读流（描述两行封顶、多行描述按原换行）、slug 退为等宽小字、元信息压成一行（作者是自己时显示「我」、相对更新时间）、订阅按钮按状态分级（未订阅描边 / 已订阅灰底绿勾悬停变「退订」/ 捆绑只读锁 / 进行中加载态）；范围筛选改成带数量的分段切换，`GET /api/skills` 信封加 `counts`（在关键词与 kind 收窄后的集合上数、scope 自身不参与）；桌面 / 移动双视口 Playwright 走查过，`skills.png` / `mobile-nav.png` 重拍。不涉及 CLI 与指南，版本号不动。**数据库分配有详情页、凭证环境与分配记录互相回指、环境清单分页筛选（决策 39）**：`/db/:id` 详情页（`GET /api/db/assignments/:id`，申请人或管理员），管理员操作与清单页共用 `pages/db-shared.tsx`；`EnvironmentInfo` 加 `source` / `dbAssignment` / `ownerEmail`（关系一直在 `db_assignment.environment_id` 上，反查暴露即可，无迁移），环境详情页头下有「查看分配」、分配详情页有「打开凭证环境」+ `eat env pull` 命令；**凭证变量默认对未授权成员完全隐藏**（`visible_without_permission=false`，存量不动）；`GET /api/envs` **破坏性地**改成分页信封 `{items,total,page,pageSize,counts}` + `q`/`source`/`page`/`pageSize`（CLI / MCP 只用 `/api/catalog`，不受影响），新增 `GET /api/envs/:slug`；控制台环境清单照 Skill 清单重做为双行清单 + 「常规 / 数据库」页签（默认常规、带数量）+ 搜索 + 分页；**更新变量时 `value` 可缺省** = 保持当前值、版本不递增（翻转敏感标记时把现值搬到另一列；新增缺省值仍 400）——此前给敏感变量改可见性会因为值必填而改不了；**Owner / 管理员可在控制台点眼睛查看敏感值**（走正式拉取接口、照常落 `secret.read` 审计）；环境详情页「读取授权」上方加 Owner 卡片（姓名 / 邮箱 / 三条能力 / 按身份补一句话）。不涉及 CLI 与指南，版本号不动。**求助对话可以删掉自己写的那条回复（决策 41）**：`DELETE /api/help-requests/:id/messages/:messageId`，回复者本人或管理员可删（管理员本就能删整条求助，这里只是粒度更细；求助已沉淀为经验时整条删不掉、单条仍可删）；删完按剩余消息回推状态——最后一条来自被求助者→`answered`，否则→`open`（一条不剩也回 `open`），`resolved`/`closed` 是终态不回退；**只在控制台开口子，CLI / MCP 不加命令**（不给 AI 一个悄悄抹掉自己发言的工具，AI 侧误发仍是 `delete_help_request` 删整条）；不推 webhook、落审计 `help.reply_deleted`（只记 messageId/senderId 不抄正文），乱写的 messageId 用 `isFullId()` 挡成 404 而不是 500。不涉及 CLI 与指南，版本号不动。**控制台可在线编辑 SKILL.md（决策 42）**：`PUT /api/skills/:slug/content`，作者或管理员在详情页直接改正文，**保存即 `currentVersion + 1`**（复用 `skill_versions` 既有机制，无迁移），订阅者下次 `eat sync` 拿到；附属文件不在这个入口改、原样沿用上一版；带 `baseVersion` 做乐观并发（对不上回 409，不覆盖别人刚推的）；正文没变回 400 不灌版本号；frontmatter 仍是元信息事实源（写了就更新 name/description，没写保持原值）；大小与密钥扫描复用 push 的 `validatePayload()`；审计仍记 `skill.version_pushed` + `via: console`。不涉及 CLI / MCP / 指南，版本号不动。**求助清单加了「待回复 / 全部」筛选（决策 43）**：与「找我的 / 我发起的」页签是两个正交维度，两个页签共用同一个值、**默认「待回复」**；「待回复」= 还等着某一方回应（`open` + `answered`），刻意包含 `answered`——「我发起的」页签角标数的就是它，只认 `open` 会出现「角标说有 2 条、点进去一条也看不到」；UI 上两个维度**必须长得不一样**（`TabsList` 与 `Segmented` 本来是同一套实心轨道样式），页签保持实心、筛选用新加的 `Segmented variant="outline"` 描边轨道，桌面一行分列左右、窄屏堆叠；页签角标 = 需要我出手的条数，分段上的小字 = 切过去有几条（按当前页签算）；筛选与页签都同步到 URL（`?status=` / `?tab=`，本页 `useTabParam` 换成 `use-query-params`）；纯前端过滤（两个清单接口本就一次取 100 条），不动服务端、不涉及 CLI / MCP / 指南，版本号不动。演示数据补了一条「吴敏 → 孙浩」的待回复求助（否则默认筛选下 `help.png` 是空的）、`capture-web.mjs` 的两张详情图改挂 `?status=all`、`help.png` 重拍。**`eat skill push` 不传 name / description 时不再覆盖平台上的原值（决策 44）**：旧行为是 CLI 拿目录名给 name 兜底、服务端无条件写回，推一个没写 frontmatter 的目录就把「运营周报生成」改成 `weekly-report`、描述抹空；现在 `pushSkillSchema` 的 `name` / `description` 可缺省，CLI 只在 `--name` / frontmatter 真读到时才带上（**目录名只用来推 slug**），服务端 `resolveMeta()` 取 `传的 → 正文 frontmatter → 原值` 第一个可用的（新建时 name 回落到 slug），与控制台在线编辑（决策 42）的「没写就保持原值」对齐。CLI 升 0.5.10、指南未变。**权限申请页有「历史审批」页签了（决策 45）**：申请记录本就永久保留在 `access_request` 表（状态 / 审批人 / 审批时间 / 有效期，随环境或申请人删除而级联删除），缺的只是入口——「待我审批」只查 `pending`、处理完就从页面消失。新增 `GET /api/access-requests/history`：范围与 inbox 同一条规则（管理员全部、其他人自己 Own 的环境，**按环境归属不按谁点的批准**，管理员代批的 Owner 也看得到），只含已处理、按审批时间倒序，分页信封 `{items,total,page,pageSize,counts}` + `status=all|approved|rejected`（`counts` 不随筛选变）；`AccessRequestInfo` 加 `decidedByName`，三个清单接口改共用一条 join 查询（去掉逐行查名字的 N+1，inbox 的 Owner 过滤也从「取回后过滤」改进 SQL）。控制台第三个页签 + 描边分段筛选（只在该页签出现）+ 分页，参数同步 URL；已批准行在状态下方显示「有效至 … / 永久有效」。CLI / MCP 不加命令（AI 不需要翻别人的审批历史），但契约进了 CLI 产物、按约定升 0.5.11；指南未变。**应用详情弹窗的 Git 仓库地址旁有「GitHub」跳转链接（决策 46）**：`githubRepoWebUrl()`（`packages/shared/src/deploy.ts`）把 GitHub 的 scp 式 SSH / `ssh://` / https（可带 `user:token@`）/ 网页地址 / 不带协议写法统一解析成 `https://github.com/owner/repo`，只认 `github.com`、其他主机回 null 不出链接；只加在点应用名打开的详情弹窗里，清单页 Git 列不加；纯前端解析、不动 `AppInfo` 与服务端，CLI 未引用（构建产物逐字节一致）、版本号不动；单测在 `apps/cli/test/git-url.spec.ts`。**内置指南的 `eat app create` 速查行补上了 `--description`**（写一句应用说明；指南升 14，CLI 未引用指南内容、构建产物不变、版本不动）。**`eat login` 的授权链接自带短码（决策 47）**：服务端 `deviceStart()` 的 `verificationUri` 拼成 `<publicUrl>/device?code=<短码>`（放服务端、旧 CLI 免升级即生效），控制台 `/device` 页本就按 `?code=` 回填、用户点开只需「确认授权」；配套把 `RequireAuth` 的登录跳转 `next` 改为 `pathname + search`，否则未登录点链接经登录页回来时 `?code=` 会丢；CLI 输出与版本不动（仍打印短码，`scripts/demo/record-cli.mjs` 靠「输入代码」这行取码）。**控制台有「已授权的设备」页了（决策 48）**：`/device/authorized`（`pages/AuthorizedDevices.tsx`）列设备码签发的 CLI Token（备注 / 授权时间 / 最近使用 / 吊销带确认），只列 `kind='cli'` 且未吊销的、网页登录会话不算设备；**清单独立成页、不和授权表单铺在一起**，`/device` 授权页只在底部加 link 风格入口「查看已授权的设备 →」；数据一直在 `api_token` 表、接口 `GET/DELETE /api/auth/tokens` 早就有，服务端不动；`apiTokenSchema` 补 `kind` 进了 CLI 产物，CLI 升 0.5.12、指南不动。**时间只在展示端按本地时区格式化（决策 49）**：给容器加 `TZ` 后页面仍显示 UTC，根因是控制台 `lib/utils.ts` 里那个 `formatDateTime` 只是 `slice` ISO 字符串（7 个页面在用），CLI 三处与服务端设备默认名同样写法；现在三端共用 `packages/shared/src/time.ts` 的 `formatDateTime()`（`YYYY-MM-DD HH:mm`）/ `formatDate()`，按所在进程的本地时区换算，**控制台只留 `lib/format.ts` 一个入口**（转发 shared 的 `formatDateTime` + 本地 `formatRelativeTime`），服务端序列化仍 `toISOString()` 不动；以后显示时间别再 slice ISO 字符串。CLI 升 0.5.13、指南不动。**MCP 配置改成两层权限：可见性 + 订阅审批（决策 50）**：可见性 `team`（全员看得到说明、可自助申请订阅）/ `private`（不公开，只有 Owner / 管理员看得到，成员只能被主动分配）；**订阅 = 提申请**，由**配置 Owner 或管理员**批准后才进 `eat sync` 范围（照抄环境变量申请的审批人口径），Owner / 管理员订自己能批的配置即时生效、角色模板派生的同样免审批；状态就存在订阅行上（`mcp_subscription` 加 `status`/`reason`/`decided_by`/`decided_at`，迁移 `0013_mcp_subscription_approval`，**存量行默认 `approved`** 不断线）——别为「申请」另开一张表，「谁订阅了」和「批没批」是同一条关系。三个坑：**已 approved 的行不能被重复点订阅打回 pending**；**`canSee` 必须把「已订阅 / 申请中」算进去**，否则分配下去的不公开配置会「同步得到却在清单里查无此物」；**审批清单只认自助申请**（`source=manual` 且不是自己批自己），否则管理员分配的、Owner 自己的订阅都会混进去。新接口：`GET /api/mcp-configs/subscription-requests?status=pending|all`、`POST …/subscription-requests/:id/decision`、`GET/POST /api/mcp-configs/:slug/subscribers`、`DELETE …/subscribers/:userId`（后四个是 Owner / 管理员，不是 `@Roles('admin')`）；`POST :slug/subscribe` body 可带 `reason`（不带 body 也要能过，schema 用 `z.preprocess` 兜 undefined——`.default({})` 不行，zod 的默认值不再过内层 schema）；`McpConfigInfo` 加 `subscriptionStatus`。控制台：审批入口是 MCP 配置页页头的「订阅申请」弹窗（待审批带圆点、默认只看待审批可切「全部」），订阅者明细与「主动分配」在每行的「订阅者」弹窗，权限申请页只加一句指引链接（不并进去：那边历史审批是服务端分页的，混第二类资源要各开一套分页接口）。CLI / MCP 不加命令，但契约进了 CLI 产物，升 0.5.14、指南未变。**MCP 配置改为经平台网关分发（决策 51）**：`http` 配置默认走网关（每个配置一个开关，存量随 DEFAULT 一次性迁入——**破坏性**，订阅者要重跑 `eat sync` 并更新客户端里的地址），成员拿到 `<平台>/mcp/<slug>/<token>`，**路径即鉴权**，上游地址与 header 不再下发（`McpConfigInfo` 的 `url`/`headers`/`env` 对非 Owner 置空）。核心是三条：**凭证按配置 Owner 的授权解析**（`EnvsService.readValuesForGateway`，只给网关用），网关模式下「订阅被批准」就是授权，成员不需要也不该有对应变量的读取权限，否则他 `eat env pull` 就能拿走凭证、网关白做；**失效靠「每次请求实时复算授权」+「每次签发必新建并吊销该 (user,config) 上所有旧 token」两条叠加**，而不是在每个改权限的地方加吊销——授权至少能从六条路径消失（退订/驳回/移除订阅者/管理员改模板内容/用户改选模板/配置删除或用户禁用），逐个加早晚漏一条，漏的那条就是永久后门 URL；这两条叠加后「取消后重新获取即换新地址、旧地址永久作废」自动成立，模板那两条路径一行代码都不用碰；**token 必须可逆存储**（信封加密，同 KEK，查询仍走 `token_hash` 唯一索引）——每次 sync / 每次开控制台都要拿回同一条地址，只存哈希 = 每次同步换地址，已配好的客户端全废。另外四个必须记住的点：签发判的是「**是否跨过有效这条线**」(`syncGatewayAccess(userId, config, wasEffective)`)，不是「有没有点过订阅」，否则已生效的人重复点订阅会把正在用的地址换掉；**SSRF 必须挡**（`upsert` 无管理员限制，任何成员都能建配置，不挡就是成员可驱动的 SSRF 跳板；解析后校验 IP、拒私网/回环/保留段、`redirect: 'manual'`，内网上游要显式 `EAT_MCP_GATEWAY_ALLOW_PRIVATE=1`）；**响应头白名单**（`www-authenticate` 按 MCP 鉴权规范带上游 resource metadata 地址，透传就漏；`set-cookie`/`location` 同理）；**旧版 HTTP+SSE 传输明确报错而不是半代理**（`endpoint` 事件里是上游地址）。**stdio 强制不走网关**（没有可代理的端点，托管进程是另一个量级的工程，本期不做）。调用记录单独建表 `mcp_gateway_call`（不写 `audit_log`：那是安全事件，这是每次工具调用一行的流量数据）、只记 method 与工具名不记 arguments、保留期默认 90 天由 `McpGatewayService` 里的每日清扫处理（server 至今没有任何调度器，pg-boss 在选型里但从未引入）。顺带修掉两个既有问题：控制台编辑弹窗**根本没有 headers 编辑器**（`toPayload` 写死 `headers: {}`，点一次「编辑」就清空配置的 header）；Fastify 对 `content-type: application/json` + 空 body 直接回 400，而 **MCP 客户端结束会话发的正是 `DELETE` + 空 body**，请求进不了网关（`common/json-body.ts` 改宽容解析器，挂在 `AppModule.onModuleInit` 上——e2e 自己造 app 不走 bootstrap，挂模块上两条路径才一致）。服务端新模块 `mcp-gateway/`（`upstream.ts` 纯函数做地址校验与头部白名单、可单测），订阅判定抽到 `mcp-configs/mcp-subscriptions.service.ts` 供两边共用（授权判定只能有一份实现）。迁移 `0014_mcp_gateway`，CLI 升 0.5.15、指南升 15。已用真上游 + 真 CLI + Playwright 双视口验证全链路。**MCP 配置的凭证归属按传输方式收敛（决策 52）**：`headers` 与 `env` 此前两种传输都能填（zod 注释还写着「两种传输都可用」），但 stdio 只能用 `env`（本地进程，没有 HTTP 请求）、http 只能用 `headers`（客户端忽略配置里的 env）。危害不是「多个没用的选项」而是**填错就静默失效**——尤其经网关分发时（网关只解析 `url` + `headers`），http 配置把 token 填进 `env`，界面上看着配好了、请求却不带凭证、上游回 401 而平台零提示；而且控制台此前只有 env 编辑器没有 headers 编辑器（决策 51 顺带补的），等于引导着人往错地方填。现在 `upsert` 按传输清空不适用的字段、`syncBundle` 只渲染用得上的那个、控制台弹窗按传输切换（stdio 给「环境变量」，http 给「请求头」+「分发方式」），schema 与契约注释一并改对。**存量数据直接清掉**，并进 `0014_mcp_gateway`（该迁移未合主干、未在任何环境跑过，不另开一条）。CLI 只有注释变化、行为不变，且 0.5.15 由决策 51 在同一分支引入、尚未发布，故**不再递增版本**（判断依据是产物有无行为变化，不是 diff 有无触到 shared）。**数据库分配建完库会把 `public` schema 归到分配账号名下（决策 53）**：`CREATE DATABASE ... OWNER x` 只改库的归属、管不到 `public`（它从 `template1` 复制来，PG 15+ 属 `pg_database_owner`、PG 14 及更早属超级用户、模板里被删过就压根没有），后果从「`\dn` 显示的不是自己、`DROP SCHEMA public` 报 must be owner」到「加固过的集群上连建表都不行」；`provisionPostgres` 建完库再连进新库，有就 `ALTER SCHEMA public OWNER TO`、没有就 `CREATE SCHEMA public AUTHORIZATION`，**改不动归属只 warn**（管理账号非超级用户时很常见），最后按 `has_schema_privilege(账号,'public','CREATE')` 判定成败、失败时把该执行的 SQL 写进 `error`。只影响新批准的申请，存量库要修得在实例上手动跑同一条 SQL。不涉及 CLI / MCP / 指南，版本号不动。**`eat login` 有非阻塞模式了（决策 54）**：设备码流程的等待是给人设计的，AI 代为执行时就是一次会话级死等（AGENT.md 里原先写的正是「阻塞等待授权」，坑是照着文档踩的）。默认行为不变（人在终端里等着自动完成更顺手），另拆出两条立即返回的命令：`eat login --no-wait` 发起并打印链接 + 短码、`eat login --status` 查一次并在通过时领取凭证；两者靠 `~/.eat/pending-login.json`（0600，deviceCode 能换 Token、按凭证对待，领到或确认失效即删、`eat logout` 一并清）串起来。四个要点：**退出码区分「还没授权」和「失败」**——`--status` 未确认时打印 `状态：pending` 且退出码 **2**（用 0 会被当成已登录，用 1 和真失败混淆），每条结果首行都带机器可读的 `状态：approved|pending|expired|none`；**阻塞模式也先落盘、超时不作废**——新增 `--timeout <秒>`（封顶不超过设备码有效期），超时 / Ctrl-C 后 `--status` 接着领，不必让用户重走浏览器；**重复发起会沿用未完成的那条授权**（剩余 ≥30 秒且同平台），否则 AI 一重试就把已转告用户的短码作废，要换新的显式 `--new`；**不做 TTY 判断**（同决策 26），要什么行为就显式写参数。服务端与设备码接口一行未改。两处 AI 入口同步改了：`/install/AGENT.md` 第 2 步改成非阻塞两步并写明退出码 2 不是失败，内置指南速查表补同样一行（指南升 16）。CLI 升 0.5.16。**eat 自身能力有标准 HTTP MCP 端点了（决策 55）**：此前只有 `eat mcp`（stdio、读本地凭证），云端 AI 服务根本连不上；现在 `POST <平台>/mcp` 是无状态 Streamable HTTP，鉴权走请求头 `Authorization: Bearer <API Key>`（`X-API-Key` 也认，**刻意不支持把密钥放 URL**——那是网关那条路的无奈，自己的端点没这个约束）。API Key 是 `api_token.kind='apikey'`，用户在控制台「安装与接入」页自助生成（明文只回一次、可设有效期、随时吊销），鉴权与守卫共用 `AuthService.authenticate()`（只能有一份实现，否则「吊销即失效」迟早在某条路径上走样），调用按 Key 主人的身份鉴权并落审计。**工具定义提到 `packages/shared/src/mcp-tools.ts` 三端共用**（CLI 映射 REST、服务端映射 Service，工具名 / 描述 / 入参只有一份）；两处刻意差异：远程 `trigger_deploy` 没有 `workdir`、部署记 `source=remote`「未做密钥扫描」（与控制台按钮同级，**不是新的绕过口子**），`get_platform_guide` 只有远程版有（云端客户端没有 `eat sync`，行为规范只能靠工具取回去；`initialize` 的 `instructions` 只放引子，不塞整份指南）。**传输层手写不引 SDK 的 `StreamableHTTPServerTransport`**：它绑 Node 原生 req/res、且 Accept 不带 `text/event-stream` 就 406，而很多云端服务只发 `application/json`——自己写才能「发出去守规范、收进来宽容」（POST 默认回 JSON、只接受 SSE 时回单事件流，通知 202、批量数组、GET 405、DELETE 204、OPTIONS 给 CORS）。合规性由 e2e 里**官方 MCP SDK 客户端**对真 listen 的实例连一次来兜底。**破坏性**：控制台「MCP 配置」页从 `/mcp` 挪到 `/mcp-configs`（`/mcp` 让给真端点，GET 要回 405 不能回退 SPA），安装页改名「安装与接入」。CLI 升 0.5.17、指南升 17。**`eat sync` 的落点现在由配置记住，并对「静默回落默认目录」设拦截（决策 56）**：坑不是缺参数（`--dir` 决策 14 就有），而是它只对当下这一次有效——更新提示里写的是裸 `eat sync`，而裸 `eat sync` 此前恒等于 `--global`，于是 `--dir` 装好的环境一收到「Skill 有变更」就被同步进 `~/.agents/skills`，原落点不再更新，且 `markSkillsSynced()` 会把基线刷成最新**让提示随之消失**，静默且自我掩盖；顺带修掉 `localSkillVersion()` 硬编码只扫 `~/.agents/skills`（`--dir` 用户没有基线时必被判「需要 sync」，而那条提示又把人引向错误落点）。解析顺序 `命令行 > 环境变量 EAT_SYNC_SCOPE/EAT_SYNC_DIR > 项目配置 ./.eat/config.json > 用户配置 ~/.eat/config.json > 内置默认`，**作用域整体覆盖不按字段合并**（否则配置的 dir 会和命令行的 project 撞上「三者互斥」校验）；**显式指定过就自动记住**（`--dir`/`--global` 写用户配置，`--project` 写项目内配置、`--no-save` 跳过）——只给 `eat config set` 让人手敲不够，安装多由 AI 代跑、装完就走；`--project` 不能写用户配置（按 cwd 解析，写全局会让换个仓库裸跑也变项目级，这是「项目配置优先」这层存在的唯一理由）。新增 `eat config list/get/set/unset`（只开 `sync.scope`/`sync.dir`，dir **一律存绝对路径**，切非 dir 作用域时丢掉 dir 免得以后复活旧目录）与 `eat sync --dry-run`（**必须同时跳过 `markSkillsSynced()` 与 `mcp.generated.json` 写入**，否则一次预演冲掉基线、真提示反而消失）；决议先于动作打印（作用域/来源/落地目录/`.claude` 去向），更新提示带上落点。**漂移检测只在回落到内置默认时触发**——最初把「配置落点与上次不同」也算漂移，结果项目目录与自定义目录来回同步时次次误报，**次次误报的拦截等于没有拦截**（用的人只会固定加 `--yes`）；预演同样不拦，它正是中止信息里给的排查办法。`eat config set/unset` 会清掉落点基线（改配置即授权），环境变量不落盘。刻意没采用「默认 dry-run + `-y`」：Agent 没有上下文知道默认落点是错的，看到正常路径照样确认下去，**拦一道不等于拦对**，且会让决策 26 的「Skill 更新执行 eat sync」变成空转。AGENT.md 第 3 步改为「先确认 skill 放哪再选命令」，指南升 18、CLI 升 0.5.18。**数据库申请放开给 AI（决策 57）**：CLI 早有 `eat db instances / request / list`，但 MCP 一个 db 工具都没有、内置指南还写着「引导用户去控制台申请」——走 HTTP MCP 的云端 AI 想给自己建个库放跑批结果，只能让人替它点界面。补 `list_db_instances` / `request_db` / `list_db_assignments` 三个工具（门禁没削弱：`request_db` 建出来仍是 `pending`，仍由管理员批），实例可用**名字**引用（`resolveDbInstance()` 在 `packages/shared/src/db.ts`，CLI 命令 / CLI stdio MCP / 服务端 HTTP MCP 三处共用，不能各写一份），名字找不到时把候选清单一起回过去省一轮往返；指南的「数据库账号」一节重写，**写明只读取数走环境变量、要自己写数据时才申请库**（否则 AI 会把「要连数据库」一律理解成「要申请一个库」）。`DbsModule` 补 `exports: [DbsService]`。CLI 升 0.5.19、指南升 19。剩余待办：真实联调（**Dokploy 已可在云端会话内用 `scripts/dev-dokploy.sh` 自建真机实例验证，`DokployClient` 的连通性/清单/状态/部署四条路径与错误路径都对着 v0.30.4 跑通**；只剩 AI 三参数待用户提供）、正式部署（Dockerfile 与 docs/deployment.md 已就绪，镜像布局在容器内做过等效验证——pnpm deploy --legacy 产物 + 迁移/种子编译入口 + SPA 托管 + CLI 产物 `/app/cli/dist` 全通，真实 docker build 需在有 Docker 的机器上做）、MySQL 自动建库（暂缓）。

**已知环境行为**：云端容器会不定期回收后台进程（postgres、node server 都可能消失，无 OOM、日志无 shutdown 记录）——重跑 `scripts/dev-db.sh start` 和重启 server 即可，不必排查。

## 常用命令

```bash
scripts/dev-db.sh start        # 本地 PostgreSQL（仅云端会话；端口 5433）
pnpm install && pnpm build     # 安装 + 全量构建（shared → server/cli/web 拓扑序）
pnpm db:migrate && pnpm db:seed  # 迁移 + 初始管理员（admin@example.com / admin12345）
pnpm --filter @eat/server test   # server e2e（连 eat_test 库，需先起数据库）
node apps/server/dist/main.js    # 启动平台（http://localhost:3000，含控制台静态托管）
node apps/cli/dist/index.js      # eat CLI（login/env list/env pull/project build-logs/mcp 等）
scripts/dev-dokploy.sh start     # 【仅云端会话】容器内起真机 Dokploy 做 API 联调（见下）
node scripts/demo/seed-demo.mjs  # 演示数据（截图 / 录屏用，见 scripts/demo/README.md）
```

- **版本号约定（手改常量，最容易漏，改代码时务必对照）**：
  - 改了 CLI（`apps/cli/**`，或 CLI 依赖的 shared 代码）→ 递增 `packages/shared/src/version.ts` 的 `CLI_VERSION`，并同步 `apps/cli/package.json` 的 `version`（CLI 单测断言两者一致，但**不检查你该升没升**）。漏升的后果：已安装的客户端收不到「有新版 CLI」提示，会一直跑旧产物且无任何报错（决策 26）。
  - 改了内置平台指南的内容（`packages/shared/src/platform-guide.ts` 的 `CONTENT`）→ 递增同文件的 `PLATFORM_GUIDE_VERSION`。漏升的后果：`eat sync` 判定本地已是最新，用户永远拿不到新内容（决策 11）。
  - 团队 Skill 的版本**不用管**：`eat skill push` 时服务端自动 `currentVersion + 1`；用户侧的「Skill 有更新」指纹也由服务端实时算出（决策 26）。
- Dokploy 联调：`scripts/dev-dokploy.sh start` 起真机（Dokploy 占 3000，平台 server 用 `PORT=3001`），控制台「系统设置 → Dokploy」填 `http://127.0.0.1:3000/api` + `scripts/dev-dokploy.sh key` 的 token。**出站代理端口每个会话都会变**，脚本会按当前环境重写 `/etc/docker/daemon.json` 并在变化时重启 dockerd，否则镜像拉取报 `proxyconnect connection refused`。
- schema 改动流程：改 `apps/server/src/db/schema.ts` → `pnpm --filter @eat/server db:generate` 生成迁移 SQL（提交进库）→ `pnpm db:migrate`。
- server 测试用 vitest + unplugin-swc（es6 模块，tsup/esbuild 不产 decorator metadata 所以必须 swc）；CLI 是 ESM + tsup 单文件（banner 里有 createRequire 垫片，勿删）。

## 本地开发（用户本机）

**仓库根有 `.env` 且配置了 `DATABASE_URL` 时，一律优先使用 `.env` 里的配置**，不要起本地 Docker 库或假设 5433 端口：

- 启动 server：`node --env-file=.env apps/server/dist/main.js`（Node 20.6+ 原生支持）。
- 跑 server e2e：测试会 **drop schema 重建**，绝不能指向 `.env` 里的业务库；用同一实例上的独立 `eat_test` 库，通过 `TEST_DATABASE_URL` 注入：

  ```bash
  set -a; source .env; set +a
  TEST_DATABASE_URL="${DATABASE_URL%/*}/eat_test" pnpm --filter @eat/server test
  ```

  `eat_test` 库不存在时先在该实例上 `CREATE DATABASE eat_test` 一次。
- 已知限制：p2.spec.ts 的 5 个「真实建库」用例硬编码登记 `127.0.0.1:5433` 本地 PG 做真实建库建号（避免在远端实例残留测试库/账号），本机没起 5433 时这 5 个用例失败属预期，其余用例应全过。
- 没有 `.env` 时才回退到 Docker 起 PostgreSQL 或连自有实例。

## 唯一事实源

`docs/product-design.md` 是产品与技术设计的唯一事实源：

- §10 决策记录：所有已拍板的决策（管理员可见求助内容、CLI 定名 eat、AI 接入 OpenAI 范式、`eat skill push`、框架选型等）。新决策产生时必须追加到这张表并同步正文。
- §7 技术选型（已定）：NestJS(Fastify) 单体 + React/Vite/Tailwind CSS v4/shadcn 风格组件 SPA + pnpm monorepo；任务队列 pg-boss（不引入 Redis）；ORM 首选 Drizzle；CLI/MCP 为 TS 单包、tsup 打包、npm 分发；平台 AI 调用采用 OpenAI 接口范式（api_base_url / api_key / model 可配）。
- §7.4 Monorepo 结构：apps/server、apps/web、apps/cli + packages/shared（zod 契约三端共用）。

## 远程开发环境须知（Claude Code on the web 容器）

**本节及「容器内直接起 PostgreSQL」的方案仅适用于 Claude Code 云端会话**。用户本地开发时不采用此方案：按上一节「本地开发（用户本机）」优先用 `.env` 配置。

- **Docker 守护进程默认没起，但可以自己拉起来**（2026-09-02 实测；此前「云端容器跑不了 Docker」的结论已作废）：容器以 root 运行、内核允许，`sysctl -w net.ipv4.ip_forward=1` + `nohup dockerd &` 即可，镜像拉取要在 `/etc/docker/daemon.json` 里配 `proxies`（代理 CA 已在系统信任库，不用额外装）。swarm、overlay 网络、容器出站全部可用。两点坑：**内核没有 IPVS**（`/proc/net/ip_vs` 不存在），swarm service 默认的 VIP 端点模式解析出的地址不通，必须 `--endpoint-mode dnsrr`；**没有 `ss` / `ip` 命令**，依赖它们做探测的第三方安装脚本会挂。
- 平台自己的业务库仍用 `scripts/dev-db.sh`（裸 PostgreSQL 更快，不必为它起 Docker）。
- **PostgreSQL 16 服务端已装**（/usr/lib/postgresql/16/bin），本地起库即可开发测试，不依赖外部数据库。已验证可用。
- 容器以 root 运行，而 Postgres 拒绝 root：需 `runuser -u postgres --` 执行，且数据目录放 postgres 用户可访问的路径（如 /var/lib/postgresql；scratchpad 的父目录是 root 700，postgres 穿不过去）。
- 云端会话统一用 `scripts/dev-db.sh start` 启动本地库（端口 5433，trust 认证，自动创建 eat_dev / eat_test），连接串：`postgres://dev@127.0.0.1:5433/eat_dev`。
- 容器是临时的：数据库数据不跨会话保留，schema 靠 Drizzle 迁移 + seed 脚本随时重建，这是预期行为。
- 出站网络走代理；用户自己的内网服务（团队数据库等）在这里不可达，那部分仍由用户本地联调。
- **Dokploy 不用等用户给地址了：`scripts/dev-dokploy.sh start` 在容器内起一台真机 Dokploy**（官方镜像 + swarm，约 3 分钟），跑完打印控制台地址、管理员账号与 API token，直接填进平台的「系统设置 → Dokploy」即可做真实联调。子命令 `status|key|logs|stop|clean`。两个必须知道的点：**不能直接用官方 install.sh**（它依赖 `ss`/`ip`，且默认 VIP 端点模式在无 IPVS 的内核上会让 dokploy 永远卡在 "Waiting for postgres"）；**API key 必须带 `metadata.organizationId`**，Dokploy 的 `validateRequest` 拿不到它就一律回 401，控制台建的 key 自带、用 better-auth 端点自己建的不带（脚本已处理，另外 better-auth 默认给 key 加 10 次/天限流，脚本直接改库关掉）。Dokploy 与平台 server 默认都占 3000，同时跑给 server 传 `PORT=3001`。

## 与用户的外部依赖约定

以下资源在需要时由用户提供，开发期一律用本地实例 / mock 先行：

| 时机 | 用户提供 |
|---|---|
| 正式部署 | Dokploy 环境 + 持久化 PostgreSQL |
| P1 联调平台 AI | api_base_url / api_key / model（OpenAI 范式） |
| P2 联调数据库账号分配 | 团队测试用 MySQL/PG 实例 |
| P3 联调 Dokploy | ~~Dokploy API 地址与 token~~ 云端会话可用 `scripts/dev-dokploy.sh` 自建真机实例，不必等用户提供；只有要对**用户那套** Dokploy 验证时才需要 |
